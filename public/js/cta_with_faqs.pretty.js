// Generated by CoffeeScript 1.10.0
if ($('.faqs_small').length) {
  (function($) {
    var setFaqHeight;
    setFaqHeight = function() {
      return $$('.faqs_small-list-item').each(function() {
        var $this, contentHeight;
        $this = $(this);
        contentHeight = $this.find('.faqs_small-list-item-content').height() + 50;
        return $this.css('height', contentHeight);
      });
    };
    $window.on('resize', util.debounce(setFaqHeight, 250));
    setTimeout(function() {
      return $$('.faqs_small-list-item').each(function() {
        var $this;
        $this = $(this);
        $this.css('height', $this.height()).addClass('closed');
        return $this.data('closed', true);
      });
    }, 50);
    return $$('.faqs_small-list-item').on('click', function() {
      var $this, closed;
      $this = $(this);
      closed = $this.data('closed');
      if (closed) {
        $this.removeClass('closed').siblings().addClass('closed').removeClass('show').data('closed', true);
        setTimeout(function() {
          return $this.addClass('show');
        }, 100);
      } else {
        $this.addClass('closed').removeClass('show');
      }
      return $this.data('closed', !closed);
    });
  })(jQuery);
}

(function($) {
  return $('.form_boxed').each(function() {
    var $form;
    $form = $(this);
    if ($form.hasClass('standard')) {
      return new Form($form, {
        'dontDisableFields': true,
        'singleStep': true
      });
    } else {
      return new Form($form);
    }
  });
})(jQuery);
